# fastapi-blog-auth-api
A FastAPI + PostgreSQL blog API with JWT authentication (OAuth2 password flow, bcrypt-hashed passwords) and full CRUD for blog posts, built with SQLAlchemy ORM and Pydantic schemas.
